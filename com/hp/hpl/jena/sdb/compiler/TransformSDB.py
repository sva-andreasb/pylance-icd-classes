def ():
    '''returns TransformSDB\n\n
    (final SDBRequest request, final QuadBlockCompiler quadBlockCompiler)\n
    '''
def transform():
    '''returns Op\n\n
    transform(final OpBGP opBGP)\n
    transform(final OpQuadPattern quadPattern)\n
    transform(final OpJoin opJoin, final Op left, final Op right)\n
    transform(final OpLeftJoin opJoin, final Op left, final Op right)\n
    transform(final OpFilter opFilter, final Op op)\n
    transform(final OpTable opTable)\n
    transform(final OpDistinct opDistinct, final Op subOp)\n
    transform(final OpProject opProject, final Op subOp)\n
    transform(final OpService opService, final Op subOp)\n
    transform(final OpSlice opSlice, final Op subOp)\n
    transform(final OpDatasetNames opDatasetNames)\n
    '''
