def createCurrentModel():
    '''public JSONObject createCurrentModel(final JSONObject weatherData, final WeatherConfig config, final UserInfo userInfo, WeatherConfig.UOM_LOCALE uomLocale)
    '''
def createDailyModel():
    '''public JSONObject createDailyModel(final JSONObject weatherData, final WeatherConfig config, final UserInfo userInfo, final WeatherConfig.UOM_LOCALE uomLocale)
    public JSONObject createDailyModel(final JSONObject weatherData, final WeatherConfig config, final UserInfo userInfo, WeatherConfig.UOM_LOCALE uomLocale, final WeatherAddress addr)
    '''
def createHourlyModel():
    '''public JSONObject createHourlyModel(final JSONObject weatherData, final WeatherConfig config, final UserInfo userInfo, WeatherConfig.UOM_LOCALE uomLocale)
    '''
def compare():
    '''public int compare(final WeatherField o1, final WeatherField o2)
    public int compare(final WeatherField o1, final WeatherField o2)
    '''
