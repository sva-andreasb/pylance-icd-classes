def withMode():
    '''returns Precision\n\n
    withMode(final MathContext mathContext)\n
    '''
def ():
    '''returns CurrencyRounderImpl\n\n
    ()\n
    (final int minFrac, final int maxFrac)\n
    (final int minSig, final int maxSig)\n
    (final int minFrac, final int maxFrac, final int minSig, final int maxSig)\n
    (final BigDecimal increment)\n
    (final BigDecimal increment, final int minFrac, final int maxFrac)\n
    (final BigDecimal increment, final int minFrac, final int maxFrac)\n
    (final Currency.CurrencyUsage usage)\n
    '''
def apply():
    '''returns None\n\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity quantity, final int minInt)\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity value)\n
    apply(final DecimalQuantity value)\n
    '''
