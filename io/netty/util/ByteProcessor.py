FIND_NUL = "ByteProcessor  new IndexOfProcessor((byte)0)"
FIND_NON_NUL = "ByteProcessor  new IndexNotOfProcessor((byte)0)"
FIND_CR = "ByteProcessor  new IndexOfProcessor((byte)13)"
FIND_NON_CR = "ByteProcessor  new IndexNotOfProcessor((byte)13)"
FIND_LF = "ByteProcessor  new IndexOfProcessor((byte)10)"
FIND_NON_LF = "ByteProcessor  new IndexNotOfProcessor((byte)10)"
FIND_SEMI_COLON = "ByteProcessor  new IndexOfProcessor((byte)59)"
FIND_COMMA = "ByteProcessor  new IndexOfProcessor((byte)44)"
FIND_ASCII_SPACE = "ByteProcessor  new IndexOfProcessor((byte)32)"
def process():
    '''returns boolean\n\n
    process(final byte value)\n
    process(final byte value)\n
    process(final byte value)\n
    process(final byte value)\n
    process(final byte value)\n
    process(final byte value)\n
    '''
def ():
    '''returns IndexNotOfProcessor\n\n
    (final byte byteToFind)\n
    (final byte byteToNotFind)\n
    '''
