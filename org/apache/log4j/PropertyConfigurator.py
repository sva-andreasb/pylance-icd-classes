def doConfigure():
    '''returns None\n\n
    doConfigure(final String configFileName, final LoggerRepository hierarchy)\n
    doConfigure(final Properties properties, final LoggerRepository hierarchy)\n
    doConfigure(final InputStream inputStream, final LoggerRepository hierarchy)\n
    doConfigure(final URL configURL, final LoggerRepository hierarchy)\n
    '''
